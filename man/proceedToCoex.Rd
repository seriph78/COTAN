% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/automaticCOTANObjectCreation.R
\name{proceedToCoex,COTAN-method}
\alias{proceedToCoex,COTAN-method}
\title{proceedToCoex}
\usage{
\S4method{proceedToCoex}{COTAN}(objCOTAN, cores, saveObj = TRUE, outDir = ".")
}
\arguments{
\item{objCOTAN}{a newly created \code{COTAN} object}

\item{cores}{number of cores to be used}

\item{saveObj}{Boolean flag; when \code{TRUE} saves intermediate analyses and
plots to file}

\item{outDir}{an existing directory for the analysis output.}
}
\value{
The updated \code{COTAN} object with genes' coex calculated. When asked,
it will also store it, along all relevant clean-plots, in the output
directory.
}
\description{
This function takes a newly created \code{COTAN} object (or the
result of a call to dropGenesCells()]) and applies all steps until the
genes' coex matrix is stored in the object
}
\examples{
data("raw.dataset")
objCOTAN <- COTAN(raw = raw.dataset)
objCOTAN <- initializeMetaDataset(objCOTAN,
                                  GEO = "code",
                                  sequencingMethod = "10X",
                                  sampleCondition = "mouse dataset")
objCOTAN <- proceedToCoex(objCOTAN, cores = 12, saveObj = FALSE)
GDI_DF <- calculateGDI(objCOTAN, statType = "S")

}
