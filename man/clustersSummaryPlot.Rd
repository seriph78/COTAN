% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustersSummaryPlot.R
\name{clustersSummaryPlot}
\alias{clustersSummaryPlot}
\title{clustersSummaryPlot}
\usage{
clustersSummaryPlot(objCOTAN, condition = NULL, clName = "", plotTitle = "")
}
\arguments{
\item{objCOTAN}{a \code{COTAN} object}

\item{condition}{name of the column in the \code{metaCells} dataframe containing
the condition. Defaults to \code{NULL}}

\item{clName}{The name of the clusterization. If not given the last available
clusterization will be used, as it is probably the most significant!}

\item{plotTitle}{The title to use for the returned plot}
}
\value{
a \code{list} of a \code{data.frame} and a \code{ggplot} objects
\itemize{
\item "data" contains the data,
\item "plot" is the returned plot
}
}
\description{
clustersSummaryPlot
}
\examples{
data("test.dataset")
objCOTAN <- automaticCOTANObjectCreation(raw = test.dataset,
                                         GEO = "S",
                                         sequencingMethod = "10X",
                                         sampleCondition = "Test",
                                         cores = 12,
                                         saveObj = FALSE,
                                         outDir = tempdir())

clusters <- cellsUniformClustering(objCOTAN, cores = 12,
                                   saveObj = FALSE,
                                   outDir = tempdir())

objCOTAN <- addClusterization(objCOTAN, clName = "clusters",
                              clusters = clusters)

dataAndPlot <- clustersSummaryPlot(objCOTAN)

}
