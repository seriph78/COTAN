% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellsUniformClustering.R
\name{cellsUniformClustering}
\alias{cellsUniformClustering}
\title{cellsUniformClustering}
\usage{
cellsUniformClustering(
  objCOTAN,
  cores = 1,
  maxIterations = 25,
  saveObj = TRUE,
  outDir = "."
)
}
\arguments{
\item{objCOTAN}{a \code{COTAN} object}

\item{cores}{number of cores (NB for windows system no more that 1 can be
used)}

\item{maxIterations}{Max number of re-clustering iterations. Defaults to 25.}

\item{saveObj}{Boolean flag; when \code{TRUE} saves intermediate analyses and
plots to file}

\item{outDir}{an existing directory for the analysis output.}
}
\value{
the new clusterization
}
\description{
The function finds a clusterizations that is \strong{uniform} by GDI
}
\details{
Once a preliminary \emph{clusterization} is obtained from \code{Seurat}
package methods, each cluster is checked for uniformity by looking at its
GDI. If The GDI is too high, the cluster is deemed \strong{non-uniform}. All
cells from \strong{non-uniform} clusters are then pooled together for another
iteration of the entire process, until all clusters are deemed \strong{uniform}.
}
\examples{
data("raw.dataset")
objCOTAN <- automaticCOTANObjectCreation(raw = raw.dataset,
                                         GEO = "S",
                                         sequencingMethod = "10X",
                                         sampleCondition = "Test",
                                         cores = 12,
                                         saveObj = TRUE,
                                         outDir = tempdir())
clusters <- cellsUniformClustering(objCOTAN, cores = 12,
                                   saveObj = TRUE,
                                   outDir = tempdir())
objCOTAN <- addClusterization(objCOTAN, clName = "clusters",
                              clusters = clusters)

}
