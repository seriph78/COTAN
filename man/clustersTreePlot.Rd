% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustersSummaryPlot.R
\name{clustersTreePlot}
\alias{clustersTreePlot}
\title{clustersTreePlot}
\usage{
clustersTreePlot(
  objCOTAN,
  kCuts,
  clName = NULL,
  distance = "cosine",
  hclustMethod = "ward.D2"
)
}
\arguments{
\item{objCOTAN}{a \code{COTAN} object}

\item{kCuts}{the number of estimated cluster (this defines the high for the
tree cut)}

\item{clName}{The name of the clusterization. If not given the last available
clusterization will be returned, as it is probably the most significant!}

\item{distance}{type of distance to use (default is \code{cosine}, \code{euclidean} is
also available)}

\item{hclustMethod}{default is "ward.D2" but can be any method defined by
[\code{\link[stats:hclust]{stats::hclust()}}] function}
}
\value{
a \code{ggplot2} object representing the dendrogram plot
}
\description{
This function returns the dendogram plot where the given
clusters are placed on the base of their relative distance
}
\examples{
data("test.dataset")
objCOTAN <- automaticCOTANObjectCreation(raw = test.dataset,
                                         GEO = "S",
                                         sequencingMethod = "10X",
                                         sampleCondition = "Test",
                                         cores = 12,
                                         saveObj = FALSE,
                                         outDir = tempdir())

clusters <- cellsUniformClustering(objCOTAN, cores = 12,
                                   saveObj = FALSE,
                                   outDir = tempdir())

objCOTAN <- addClusterization(objCOTAN, clName = "clusters",
                              clusters = clusters)

treePlot <- clustersTreePlot(objCOTAN, 2)

}
