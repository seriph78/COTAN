% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GDI-plot.R
\name{GDIPlot}
\alias{GDIPlot}
\title{GDIPlot}
\usage{
GDIPlot(
  objCOTAN,
  genes,
  cond = "",
  statType = "S",
  GDIThreshold = 1.5,
  GDIIn = NULL
)
}
\arguments{
\item{objCOTAN}{a \code{COTAN} object}

\item{genes}{a named \code{list} of genes to label. Each array will have different
color.}

\item{cond}{a string corresponding to the condition/sample (it is used only
for the title).}

\item{statType}{type of statistic to be used. Default is "S": Pearson's
chi-squared test statistics. "G" is G-test statistics}

\item{GDIThreshold}{the threshold level that discriminates uniform clusters.
It defaults to \eqn{1.5}}

\item{GDIIn}{when the GDI data frame was already calculated, it can be put
here to speed up the process (default is \code{NULL})}
}
\value{
A \code{ggplot2} object
}
\description{
This function directly evaluate and plot the GDI for a sample.
}
\examples{
data("test.dataset")
objCOTAN <- COTAN(raw = test.dataset)
objCOTAN <- proceedToCoex(objCOTAN, cores = 12, saveObj = FALSE)
genes <- c("g-000010", "g-000020", "g-000030", "g-000300", "g-000330"),
           "g-000510", "g-000530", "g-000550", "g-000570", "g-000590")
gdiPlot <- GDIPlot(objCOTAN, genes = genes, cond = "raw")
plot(gdiPlot)

}
