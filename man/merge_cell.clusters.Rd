% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_cell_clusters.R
\name{merge_cell.clusters}
\alias{merge_cell.clusters}
\alias{merge_cell.clusters,scCOTAN-method}
\title{merge_cell.clusters}
\usage{
merge_cell.clusters(
  obj,
  cond,
  cores = 1,
  out_dir_root,
  srat,
  out_dir,
  GEO,
  sc.method,
  mt = FALSE,
  mt_prefix = "^mt",
  markers = NULL
)

\S4method{merge_cell.clusters}{scCOTAN}(
  obj,
  cond,
  cores = 1,
  out_dir_root,
  srat,
  out_dir,
  GEO,
  sc.method,
  mt = FALSE,
  mt_prefix = "^mt",
  markers = NULL
)
}
\arguments{
\item{obj}{COTAN object}

\item{cond}{sample condition name}

\item{cores}{number cores used}

\item{out_dir_root}{path to the directory in which there is the Seurat object}

\item{srat}{the Seurat object for the same data set (the name should be "cond")}

\item{out_dir}{path to a directory for output}

\item{GEO}{GEO or other data set code}

\item{sc.method}{scRNAseq method}

\item{mt}{T or F if the mitochondrial genes should be kept or removed (default F)}

\item{mt_prefix}{Prefix for the mitochondrial genes}

\item{markers}{a list of marker genes. Default NULL}
}
\value{
a COTAN object
}
\description{
This function takes in input a COTAN object with an already homogeneous clusters
and, through the cosine distance and the hclust (with ward.D2 method), checks it merging
two leaf clusters it is formed a still homogeneous cluster (this is done iteratively).
All structures are saved on disk.
}
