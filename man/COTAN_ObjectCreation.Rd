% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R, R/automaticCOTANObjectCreation.R
\name{COTAN_ObjectCreation}
\alias{COTAN_ObjectCreation}
\alias{COTAN}
\alias{proceedToCoex,COTAN,missing,missing-method}
\alias{proceedToCoex}
\alias{proceedToCoex,COTAN,ExecutionOptions,missing-method}
\alias{proceedToCoex,COTAN,missing,CleaningOptions-method}
\alias{proceedToCoex,COTAN,ExecutionOptions,CleaningOptions-method}
\alias{automaticCOTANObjectCreation}
\title{\code{COTAN} shortcuts}
\usage{
COTAN(raw = "ANY")

\S4method{proceedToCoex}{COTAN,missing,missing}(
  objCOTAN,
  calcCoex = TRUE,
  optimizeForSpeed = TRUE,
  deviceStr = "cuda",
  cores = 1L,
  cellsCutoff = 0.003,
  genesCutoff = 0.002,
  cellsThreshold = 0.99,
  genesThreshold = 0.99,
  cleaningOptions = NULL,
  saveObj = FALSE,
  outDir = ".",
  executionOptions = NULL
)

\S4method{proceedToCoex}{COTAN,ExecutionOptions,missing}(
  objCOTAN,
  calcCoex = TRUE,
  optimizeForSpeed = TRUE,
  deviceStr = "cuda",
  cores = 1L,
  cellsCutoff = 0.003,
  genesCutoff = 0.002,
  cellsThreshold = 0.99,
  genesThreshold = 0.99,
  cleaningOptions = NULL,
  saveObj = FALSE,
  outDir = ".",
  executionOptions = NULL
)

\S4method{proceedToCoex}{COTAN,missing,CleaningOptions}(
  objCOTAN,
  calcCoex = TRUE,
  optimizeForSpeed = TRUE,
  deviceStr = "cuda",
  cores = 1L,
  cellsCutoff = 0.003,
  genesCutoff = 0.002,
  cellsThreshold = 0.99,
  genesThreshold = 0.99,
  cleaningOptions = NULL,
  saveObj = FALSE,
  outDir = ".",
  executionOptions = NULL
)

\S4method{proceedToCoex}{COTAN,ExecutionOptions,CleaningOptions}(
  objCOTAN,
  calcCoex = TRUE,
  optimizeForSpeed = TRUE,
  deviceStr = "cuda",
  cores = 1L,
  cellsCutoff = 0.003,
  genesCutoff = 0.002,
  cellsThreshold = 0.99,
  genesThreshold = 0.99,
  cleaningOptions = NULL,
  saveObj = FALSE,
  outDir = ".",
  executionOptions = NULL
)

automaticCOTANObjectCreation(raw, GEO, sequencingMethod, sampleCondition, ...)
}
\arguments{
\item{raw}{a matrix or dataframe with the raw counts}

\item{objCOTAN}{a newly created \code{COTAN} object}

\item{calcCoex}{a Boolean to determine whether to calculate the genes' \code{COEX}
or stop just before at the \code{\link[=estimateDispersionViaSolver]{estimateDispersionViaSolver()}} step}

\item{optimizeForSpeed}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy execution
scalar. When \code{TRUE}, \code{COTAN} tries to use accelerated \code{torch}-based matrix
calculations when available; otherwise it falls back to the slower legacy
code. Use
\code{executionOptions = ExecutionOptions(optimizeForSpeed = ...)} instead.}

\item{deviceStr}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy execution scalar.
Requested \code{torch} device string, for example \code{"cpu"}, \code{"cuda"}, or
\code{"cuda:0"}. Use \code{executionOptions = ExecutionOptions(deviceStr = ...)}
instead.}

\item{cores}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy execution scalar.
Requested number of CPU cores. The effective value is bounded by the
available cores. Use \code{executionOptions = ExecutionOptions(cores = ...)}
instead.}

\item{cellsCutoff}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy cleaning scalar.
\code{clean()} deletes from the \code{raw} data any gene expressed in fewer cells than
this fraction times the total number of cells. Default cutoff is
\eqn{0.003 \; (0.3\%)}. Use
\code{cleaningOptions = CleaningOptions(cellsCutoff = ...)} instead.}

\item{genesCutoff}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy cleaning scalar.
\code{clean()} deletes from the \code{raw} data any cell expressing fewer genes than
this fraction times the total number of genes. Default cutoff is
\eqn{0.002 \; (0.2\%)}. Use
\code{cleaningOptions = CleaningOptions(genesCutoff = ...)} instead.}

\item{cellsThreshold}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy cleaning
scalar. Any gene expressed in more cells than this fraction times the total
number of cells is marked as \strong{fully-expressed}. Default threshold is
\eqn{0.99 \; (99.0\%)}. Use
\code{cleaningOptions = CleaningOptions(cellsThreshold = ...)} instead.}

\item{genesThreshold}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Legacy cleaning
scalar. Any cell expressing more genes than this fraction times the total
number of genes is marked as \strong{fully-expressing}. Default threshold is
\eqn{0.99 \; (99.0\%)}. Use
\code{cleaningOptions = CleaningOptions(genesThreshold = ...)} instead.}

\item{cleaningOptions}{A \code{CleaningOptions} object bundling cleaning cutoffs
and fully-expressed / fully-expressing thresholds. This is the preferred
interface for new code.}

\item{saveObj}{Boolean flag; when \code{TRUE} saves intermediate analyses and
plots to file}

\item{outDir}{an existing directory for the analysis output.}

\item{executionOptions}{An \code{ExecutionOptions} object bundling the execution
controls. This is the preferred interface for new code.}

\item{GEO}{a code reporting the GEO identification or other specific dataset
code}

\item{sequencingMethod}{a string reporting the method used for the sequencing}

\item{sampleCondition}{a string reporting the specific sample condition or
time point}

\item{...}{Additional arguments forwarded to \code{\link[=proceedToCoex]{proceedToCoex()}}}
}
\value{
a \code{COTAN} object

\code{proceedToCoex()} returns the updated \code{COTAN} object with genes'
\code{COEX} calculated. If asked to, it will also store the object, along all
relevant clean-plots, in the output directory.

\code{automaticCOTANObjectCreation()} returns a new \code{COTAN} object after
initialization and analysis via \code{\link[=proceedToCoex]{proceedToCoex()}}.
}
\description{
These functions create a \link[=COTAN-class]{COTAN} object and/or also run
all the necessary steps until the genes' \code{COEX} matrix is calculated.
}
\details{
Constructor of the class \code{COTAN}

\code{proceedToCoex()} takes a newly created \code{COTAN} object (or the
result of a call to \code{dropGenesCells()}) and runs \code{\link[=calculateCoex]{calculateCoex()}}

Alternative interface using an \code{ExecutionOptions} object.

Alternative interface using a \code{CleaningOptions} object.

Alternative interface using \code{ExecutionOptions} and \code{CleaningOptions}
objects.

\code{automaticCOTANObjectCreation()} creates a \code{COTAN} object,
initializes its dataset metadata, and then calls \code{\link[=proceedToCoex]{proceedToCoex()}} on it.
}
\section{Lifecycle}{

Legacy scalar cleaning and execution arguments are soft-deprecated as of
COTAN 2.13.3. Use \code{cleaningOptions = CleaningOptions(...)} and
\code{executionOptions = ExecutionOptions(...)} in new code.
}

\section{Forwarded arguments}{

Additional arguments are forwarded to \code{\link[=proceedToCoex]{proceedToCoex()}}. Typical forwarded
arguments include \code{calcCoex}, \code{cleaningOptions}, \code{executionOptions},
\code{saveObj} and \code{outDir}.

Legacy scalar cleaning and execution arguments accepted by \code{\link[=proceedToCoex]{proceedToCoex()}}
are still forwarded for compatibility, but are soft-deprecated as of COTAN
2.13.3.
}

\examples{
data("test.dataset")
obj <- COTAN(raw = test.dataset)

options(parallelly.fork.enable = TRUE)

#
# In case one needs to run more steps to clean the datatset
# the following might apply
if (FALSE) {
  objCOTAN <- initializeMetaDataset(objCOTAN,
                                    GEO = "test",
                                    sequencingMethod = "artificial",
                                    sampleCondition = "test dataset")
#
# doing all the cleaning and analysis...
#
  exec <- ExecutionOptions(cores = 6L,
                           optimizeForSpeed = TRUE,
                           deviceStr = "cuda",
                           chunkSize = 1024L)
  cleanOpt <- CleaningOptions()

# in case the genes' `COEX` is not needed it can be skipped
# (e.g. when calling [cellsUniformClustering()])
#
  objCOTAN <- proceedToCoex(
    objCOTAN,
    calcCoex = FALSE,
    executionOptions = exec,
    cleaningOptions = cleanOpt,
    saveObj = FALSE
  )
}


## Otherwise it is possible to run all at once.
exec <- ExecutionOptions(cores = 6L,
                         optimizeForSpeed = TRUE,
                         deviceStr = "cuda",
                         chunkSize = 1024L)
cleanOpt <- CleaningOptions()

objCOTAN <- automaticCOTANObjectCreation(
  raw = test.dataset,
  GEO = "code",
  sequencingMethod = "10X",
  sampleCondition = "mouse_dataset",
  calcCoex = TRUE,
  executionOptions = exec,
  cleaningOptions = cleanOpt,
  saveObj = FALSE
)

}
